using System;

class MainClass
{
  public static void Main (string[] args)
  {
    int first = 4;
    int second = 5;
    
    double squared = Math.Pow(first, 2) + Math.Pow(second, 2);
    double answer = Math.Sqrt(firstAns);
    Console.WriteLine(answer);
  }
}
